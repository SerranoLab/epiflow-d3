api/R/data/
certs/
.DS_Store
docker-compose.override.yml

# Large data files (not versioned)
/*.rds
# Installer backups
api/R/*.bak.*
# Titration installer scaffolding (endpoints now live in plumber.R)
titration_endpoints_block.R
install_titration_endpoints.sh
titration_endpoints.patch
make_test_condition_rds.R
test_condition.rds
# Never commit certs / server-only dirs
/certs/
/certbot-webroot/
# local working artifacts
*.zip
epiflow_d3_phenotype_only_*.patch
PHENOTYPE_ONLY_APPLY.md
